apiVersion: v1
kind: Secret
metadata:
  name: {{ include "proxy-coordinator.fullname" . }}-secrets
  labels:
    {{- include "proxy-coordinator.labels" . | nindent 4 }}
type: Opaque
data:
  coordinator-private-key: {{ .Values.config.coordinatorPrivateKey | b64enc | quote }}

