# Prometheus configuration for Farcaster Hubble monitoring
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'hubble'
    static_configs:
      - targets: ['hubble:2281']
    metrics_path: /metrics

