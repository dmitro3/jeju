[test]
# Preload test setup to ensure infrastructure is running
preload = ["./tests/setup.ts"]

# Test timeout (30 seconds)
timeout = 30000

# Retry flaky tests once
retry = 1

# Test paths - only run src and tests, not frontend (those use Playwright)
root = ["src", "tests"]
